Добавить в раздел «Оформление праздников» новую услугу с расширенной карточкой: фото-обложка, описание, цена, **галерея из нескольких фото** и кнопка «Оставить заявку».

### Изменения

1. **`src/data/decorServices.ts`**
   - Расширить тип `DecorService` опциональным полем `gallery?: string[]` (массив дополнительных фото).
   - У существующих двух арок добавить `gallery` из имеющихся фото (`decor-arch-white.jpg`, `decor-arch-pastel.jpg`).
   - Добавить новую услугу `decor-3` — «Романтическая фотозона из роз и гипсофилы»:
     - категория: «Свадьбы»
     - цена: 65000
     - image: `decor-arch-white.jpg` (обложка)
     - gallery: `[decor-arch-white.jpg, decor-arch-pastel.jpg]`
     - описание про фотозону для церемонии и торжества.

2. **`src/components/DecorCard.tsx`** — добавить мини-галерею
   - Под главным фото (или поверх — превью-полоска) показывать до 3 миниатюр из `service.gallery`, если массив есть.
   - Клик по миниатюре меняет главное фото в карточке (локальный `useState`).
   - Если `gallery` пустая — карточка выглядит как сейчас.

3. **`src/components/admin/DecorTab.tsx`** — поддержка галереи в админке
   - В форме добавить блок «Галерея»: загрузка нескольких файлов (input `multiple`), превью загруженных фото с кнопкой удаления.
   - Сохранять как `string[]` (data-URL), как уже делается для основного `image`.

### Технические детали

- Поле `gallery` опциональное → существующий код и админка не ломаются.
- Кнопка «Оставить заявку» и модалка `DecorRequestModal` уже есть — переиспользуем без изменений.
- Все стили — из существующей дизайн-системы (`gold`, `font-heading`, `font-body`, `rounded-sm`).
- Файлы изображений уже в `src/assets/` — новые ассеты не нужны.