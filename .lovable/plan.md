
Добавить кнопку-ссылку «Магия Роз — Конструктор букета» в шапку админ-панели (`src/pages/AdminPage.tsx`), рядом с заголовком и кнопкой «Выйти».

### Изменения

**`src/pages/AdminPage.tsx`** — в блоке header (где сейчас заголовок и «Выйти») добавить `<Link to="/constructor">` с иконкой `Sparkles` и текстом «Конструктор букета». Стиль: outline-кнопка с золотым акцентом, чтобы выделялась.

```
[Админ-панель]      [✨ Конструктор букета]  [Выйти →]
```

Импортировать `Link` из `react-router-dom` и `Sparkles` из `lucide-react`.
