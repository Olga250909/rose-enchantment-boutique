Добавить в админ-панель новую вкладку «Оформление праздников» с возможностью создавать, редактировать и удалять карточки услуг оформления (фото, название, описание, цена, категория). Стартово — 2 карточки свадебных арок из загруженных фото.

### Изменения

1. **Активы** — скопировать фото:
   - `user-uploads://ChatGPT_Image_Apr_24_2026_09_58_10_PM.png` → `src/assets/decor-arch-white.jpg`
   - `user-uploads://ChatGPT_Image_Apr_24_2026_09_59_15_PM.png` → `src/assets/decor-arch-pastel.jpg`

2. **Новый файл `src/data/decorServices.ts`** — тип `DecorService { id, name, description, price, category, image }` и `initialDecorServices` с 2 карточками:
   - «Свадебная арка „Белоснежная нежность“» — категория «Свадьбы», 85 000 ₽, фото `decor-arch-white.jpg`. Описание: «Романтичная арка из белых роз и облаков гипсофилы с акцентами нежно-розовых бутонов. Идеальное обрамление для церемонии и фотозоны.»
   - «Свадебная арка „Пастельный рассвет“» — категория «Свадьбы», 95 000 ₽, фото `decor-arch-pastel.jpg`. Описание: «Воздушная арка из пастельных кремовых и пудрово-розовых роз с гипсофилой. Утончённое оформление церемонии в романтичной палитре.»

3. **`src/context/StoreContext.tsx`** — добавить состояние и методы:
   - `decorServices: DecorService[]` (init из `initialDecorServices`)
   - `addDecorService`, `updateDecorService`, `deleteDecorService` (по аналогии с продуктами)
   - Расширить `StoreContextType` и провайдер.

4. **Новый компонент `src/components/admin/DecorTab.tsx`** — копия логики `ProductsTab.tsx`, адаптированная под `DecorService`:
   - Поля формы: название, описание, цена, категория (Свадьбы / Юбилеи / Корпоративы / Детские праздники / своя), фото (загрузка через FileReader → base64, лимит 5 МБ).
   - Сетка карточек с кнопками «Редактировать» и «Удалить», тосты как в ProductsTab.

5. **`src/pages/AdminPage.tsx`** — добавить вкладку:
   - Импорт `DecorTab` и иконки `Sparkles` (или `PartyPopper` из lucide-react).
   - В массив `tabs` пункт `{ key: "decor", label: "Оформление праздников", icon: <PartyPopper/>, count: decorServices.length }`.
   - Тип `Tab` дополнить `"decor"`, отрендерить `{tab === "decor" && <DecorTab />}`.

### Технические детали

- Хранение — в `StoreContext` (in-memory, как товары). Постоянного хранения в БД сейчас не делаем.
- Публичной страницы для услуг не создаём — только админ-управление (по ответу на 1-й вопрос: CRUD каталог; публичная страница не запрашивалась явно, можно добавить отдельной задачей).
- Фото в карточках хранятся как base64 (новые загрузки) или путь к импорту из `src/assets` (стартовые).